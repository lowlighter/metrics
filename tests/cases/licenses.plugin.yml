- name: 📜 Repository licenses - Licenses and permissions
  with:
    token: MOCKED_TOKEN
    template: repository
    repo: metrics
    plugin_licenses: 'yes'
    plugin_licenses_setup: npm ci
  timeout: 1800000
  modes:
    - action
