# Note: this schema is subject to change upon official release
name: draft
properties:
  schema:
    description: Schema identifier
    type: "'draft'"
    required: true
  with:
    description: Preset options
    type: "{[key:string]:unknown}"
