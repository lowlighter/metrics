query Repository {
  user(login: "$login") {
    repository(name: "$repository") {
      $type(first: 100) {
        pageInfo {
          hasNextPage
          endCursor
        }
        nodes {
          avatarUrl(size: 24)
          login
        }
      }
    }
  }
}