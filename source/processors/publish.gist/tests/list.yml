- name: can publish text files
  plugins:
    - processors:
      - inject.content:
          content: hello world
      - render:
          format: svg
      - publish.gist:
          gist: 1234
          filename: test.*

- name: can publish image files
  plugins:
    - processors:
      - inject.content:
          content: hello world
      - render:
          format: png
      - publish.gist:
          gist: 1234
          filename: test.*
