- name: can write text files
  plugins:
    - processors:
      - inject.content:
          content: hello world
      - render:
          format: svg
      - publish.file:
          filepath: .test/publish.file.*

- name: can write image files
  plugins:
    - processors:
      - inject.content:
          content: hello world
      - render:
          format: png
      - publish.file:
          filepath: .test/publish.file.*
