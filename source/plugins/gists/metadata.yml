name: "🎫 Gists"
cost: 1 GraphQL request per 100 gists
supports:
  - user
inputs:

  # Enable or disable plugin
  plugin_gists:
    description: Display gists metrics
    type: boolean
    default: no
